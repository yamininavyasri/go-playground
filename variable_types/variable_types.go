package main

import (
	"fmt"
)

func main() {
	var x int = 100
	var name string = "navya"
	y := 1.0

	// Print with 1 decimal for float
	fmt.Printf("Declared variables are: %d, %s, %f\n", x, name, y)
}
